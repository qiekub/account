{
  "license": "UNLICENSED",
  "dependencies": {},
  "engines": {
    "node": "10"
  },
  "scripts": {
    "serve": "firebase serve --only hosting -p 4000 -o $(ifconfig | grep -Eo 'inet (addr:)?([0-9]*\\.){3}[0-9]*' | grep -Eo '([0-9]*\\.){3}[0-9]*' | grep -v '127.0.0.1')",
    "deploy": "firebase deploy --only hosting"
  }
}
